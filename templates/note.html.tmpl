<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ .Title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="/css/retro-sci-fi.css?v=12">
    <link rel="icon" href="/favicon.ico?v=2" sizes="any">
    <link rel="icon" type="image/png" href="/favicon-32x32.png?v=2" sizes="32x32">
    <link rel="icon" type="image/png" href="/favicon-16x16.png?v=2" sizes="16x16">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png?v=2" sizes="180x180">
    <link rel="manifest" href="/site.webmanifest?v=2">
    <meta name="theme-color" content="#0a0e1a">

</head>
<body>
  <div class="wrap">
    <div class="crt">
      <header>
        <h1>{{ .Title }}</h1>
        <p class="byline">By {{ .Author.Name }} · <time datetime="{{ .Date }}">{{ .DateHuman }}</time></p>
        {{- if .Source }}
        <p class="source">Source: {{ if isURL .Source }}<a href="{{ deref .Source }}">{{ deref .Source }}</a>{{ else }}{{ deref .Source }}{{ end }}</p>
        {{- end }}
      </header>

      <div class="rule" aria-hidden="true"></div>
      <nav class="site-nav">
        <a href="/">Home</a> · <a href="/archive/">Post Archive</a> · <a href="/notes/">Notes Archive</a>
      </nav>
       <article>
          {{ .ContentHTML }}
      </article>

    {{- if .Tags }}
      <nav class="tags" aria-label="Tags">
        {{- range .Tags }}
          <a class="tag" href="/tag/{{ .Slug }}/">{{ .Name }}</a>
        {{- end }}
      </nav>
    {{- end }}

      <footer>
        <a href="/">Home</a> · <a href="/archive/">Post Archive</a> · <a href="/notes/">Notes Archive</a>
      </footer>
    </div>
  </div>
</body>
</html>
