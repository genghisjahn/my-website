<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ .Title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/retro-sci-fi.css">
</head>
<body>
  <div class="wrap">
    <div class="crt">
      <header>
        <h1>{{ .Title }}</h1>
        {{- if .Subtitle }}<p class="byline">{{ .Subtitle }}</p>{{ end }}
      </header>
      <div class="rule" aria-hidden="true"></div>
      <article>
        <ul>
          {{- range .Items }}
          <li><time datetime="{{ .ISODate }}">{{ .HumanDate }}</time> · <a href="{{ .URL }}">{{ .Title }}</a></li>
          {{- end }}
        </ul>
      </article>
      <footer>
        <a href="/">Home</a>
      </footer>
    </div>
  </div>
</body>
</html>
